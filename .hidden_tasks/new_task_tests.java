Create a test class for each of the classes `Game`, `Player`, `Enemy`. The test class should verify:

- the state of `Player` and `Enemy` objects are correctly set during initialization.
- the state of the `Player` and `Enemy` objects change correctly when their respective methods are called.
- the state of the `Player` and `Enemy` objects change correctly when `startGame` method is called.